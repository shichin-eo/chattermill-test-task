import React from "react";
import { SLoader, SWrapper } from "./Spinner.styled";

const Spinner = () => {
  return (
    <SWrapper>
      <SLoader></SLoader>
    </SWrapper>
  );
};

export default Spinner;
